/**
 * In the lecture video, it was mentioned to just use the import java.util.Iterator; instead of building one. However, for future reference it was said that we may need to eventually build one.
 * this was suppose to check if within the array, the current position had an index following, then next moves the position up one.
 * @author Kyrtis Scott
 *
 */

public interface Iterator {
	
	//The video said to just use the built in iterator: java.util.Iterator; instead of building one.
	
	/**
	 * This would check and return if the position in the array has an index after the current index position.
	 */
	//public boolean hasNext() {
		
	//}
	
	/**
	 * This would move the current position within the array to the next index if hasNext was true.
	 */
	//public Object next() {
		
	//}
}
